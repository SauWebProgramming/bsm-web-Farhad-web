@model Ciftlikten.Models.Authority

@{
    ViewBag.Title = "Create";

    Layout = "~/Views/Shared/AdminLayout.cshtml";
}


<div class="container-fluid">
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="card shadow mb-4">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Yeni Yetki oluştur</h6>
            </div>
            <div class="card-body">
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    <label>Yetki Adı:</label>
                    <div class="col-md-10">
                        @Html.EditorFor(model => model.authorityName, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.authorityName, "", new { @class = "text-danger" })
                    </div>
                </div>

            </div>
            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Oluştur" class="btn btn-outline-success" /> |
                    @Html.ActionLink("Geri Dön", "Index", null, new { @class = "btn btn-outline-secondary" })
                </div>
            </div>
        </div>
    }
</div>